<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.score.dao.PredictMapper">
    <resultMap id="BaseResultMap" type="com.score.bean.Plc">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="plc_id" jdbcType="INTEGER" property="plcId" />
        <result column="dj_no" jdbcType="VARCHAR" property="plcdjNo" />
        <result column="time" jdbcType="VARCHAR" property="plcTime" />
        <result column="spindle_load" jdbcType="VARCHAR" property="spindleLoad" />
        <result column="x" jdbcType="VARCHAR" property="plcx" />
        <result column="y" jdbcType="VARCHAR" property="plcy" />
        <result column="z" jdbcType="VARCHAR" property="plcz" />

    </resultMap>

    <insert id="addUser" parameterType="com.score.bean.Plc">
    insert into t_plc
    (plc_id,dj_no,ptime,spindle_load,x,y,z)
    values
    (
    #{plcId},
    #{plcdjNo},
    #{plcTime},
    #{spindleLoad},
    #{plcx},
    #{plcy},
    #{plcz}
    )
  </insert>
    <update id="updateUserById" parameterType="com.score.bean.Plc">
    update t_plc
    set
    dj_no=#{plcdjNo},
    ptime=#{plcTime},
    spindle_Load#{spindleLoad},
    x=#{plcx},
    y=#{plcy},
    z=#{plcz}
    where plc_id = #{plcId}
  </update>

    <select id="selectById" resultType="java.lang.Integer">
    SELECT
    count(*)
    FROM t_plc
    WHERE plc_id=#{plcId}
  </select>

</mapper>